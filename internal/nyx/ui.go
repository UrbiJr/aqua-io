package nyx

// UI contains fyne elements
type UI struct {
}

// MakeUI instantiates all the needed pages and makes the UI layout, but does not display it yet.
func (nyx *Config) MakeUI() {

	// add application tabs (home, tasks, proxies, billing, settings)

	// create  toolbar (notifications, user) at the same level of application tabs

	// populate app tabs

}
